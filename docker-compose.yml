services:
  kiwi-bot:
    build: https://github.com/TheOksigen/kiwi.git
    restart: unless-stopped
    environment:
      - BOT_TOKEN=BURAYA_BOT_TOKEN_YAZ
      - YOUTUBE_COOKIE=BURAYA_COOKIE_YAZ
    volumes:
      - kiwi-temp:/app/temp

volumes:
  kiwi-temp:
